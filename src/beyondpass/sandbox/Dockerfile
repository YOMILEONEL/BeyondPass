FROM python:3.12-slim

# Kein zusaetzliches Package fuer HumanEval noetig (Standardbibliothek reicht).
# Ausfuehrung erfolgt bewusst als vorhandener, nicht-privilegierter "nobody"-User
# (per --user zur Laufzeit gesetzt, siehe docker_runner.py) statt als root.
